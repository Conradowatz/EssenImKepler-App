<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:id="@+id/loadingData_layout"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="de.conradowatz.essenimkepler.LoadingDataActivity">

    <ProgressBar
        android:id="@+id/loadingData_progressBar"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerInParent="true"/>

    <ImageView
        android:id="@+id/loadingData_logo_imageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:src="@drawable/logo"/>

    <TextView
        android:id="@+id/loadingData_laden_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="Lade Daten..."
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp"/>

    <ImageView
        android:id="@+id/loadingData_cloud_imageView"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:src="@drawable/cloud"
        android:scaleType="centerInside"
        android:layout_marginTop="100dp"
        android:layout_centerHorizontal="true"
        android:visibility="invisible"/>

    <TextView
        android:id="@+id/loadingData_noconnection_textView"
        android:text="Keine Verbindung zum Server. \nZum erneut Versuchen tippen."
        android:layout_centerHorizontal="true"
        android:layout_below="@id/loadingData_cloud_imageView"
        android:textColor="@color/warn_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:textStyle="bold|italic"
        android:textSize="20sp" />


</RelativeLayout>
